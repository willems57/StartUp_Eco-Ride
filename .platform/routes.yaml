"https://{default}/":
    type: upstream
    upstream: "frontend:http"
    cache:
        enabled: false

"https://api.{default}/":
    type: upstream
    upstream: "backend:http"
    cache:
        enabled: false
