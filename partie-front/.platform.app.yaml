name: front
type: nodejs:16 # Version de Node.js, adaptez si besoin
web:
  locations:
    "/":
      root: "build" # Dossier contenant les fichiers buildés (pour React/Angular)
      index: "index.html"
build:
  commands:
    - npm install
    - npm run build
